version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - redis
    environment:
      - REDIS_ADDR=redis:6379
      - GOPROXY=https://goproxy.cn,direct
  redis:
    image: redis:7-alpine
